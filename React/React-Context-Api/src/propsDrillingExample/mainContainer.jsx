import React from "react";
import CardContainer from "./cardContainer";

export default function MainContainer({ text }) {
  return (
    <div>
      mainContainer
      <CardContainer text={text} />
    </div>
  );
}
